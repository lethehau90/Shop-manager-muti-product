@model IList<Web_Shop_SettingIphone.Models.Entity.GroupMenuSanPham>
@if (Model.Count > 0)
{
    foreach (var item in Model)
            {
                @:&nbsp; &nbsp;  <input type="checkbox" name="Ck_Sanphamcungloai" value="@item.Tag" /> @item.Name  <br />
                {
                    Html.RenderAction("MenuLeverMuti_lever3", "CategotyProductDactinh", new { id = item.Id, select = ViewBag.select });
                }
            }
}