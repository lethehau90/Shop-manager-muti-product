@model IList<Web_Shop_SettingIphone.Models.Entity.Kichthuoc>
@if (Model.Count > 0)
{
    <div class="col-lg-2 col-md-2 col-sm-2">
        <div class="form-group">
            <label class="kichco">Kích cỡ</label><br />
            @foreach (var item in Model)
{
    <input type="checkbox" name="Ck_Kichthuoc"  value="@item.Kichthuoc1" /> @item.Kichthuoc1 <br />
            }
        </div>
    </div>
}