@model IList<Web_Shop_SettingIphone.Models.Entity.DientuMenuSitemathang>
@if (Model.Count > 0)
{
    <ul class="nav nav-third-level" style="padding-left: 20px;">
        @foreach (var item in Model)
        {
            <li>
                <a href="@Url.Action("Read", "DientuMathangAdmin", new { tag = @item.Tag })">@item.MenuName</a>
            </li>
        }

    </ul>
}