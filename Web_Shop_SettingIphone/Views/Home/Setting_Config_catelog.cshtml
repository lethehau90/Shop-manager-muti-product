@model IList<Web_Shop_SettingIphone.Models.Entity.Config>
@if (Model.Count > 0)
{
    foreach (var item in Model)
    {
        <meta name="description" content="@item.Description" />
        <meta name="keywords" content="@item.Keyword" />
    }
}